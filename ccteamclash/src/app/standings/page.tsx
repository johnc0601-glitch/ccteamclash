import {teams} from '@/lib-data';import {SiteHeader,Footer} from '@/components/SiteHeader';export default function Page(){return <><SiteHeader/><main className="shell page-shell"><span className="eyebrow">2026 SEASON</span><h1>Standings</h1><div className="standings-card full"><div className="table-head"><span>Rank / Team</span><span>Record</span><span>Diff.</span></div>{teams.map((t,i)=><div className="standings-row" key={t.name}><span><b>{i+1}</b>{t.name}</span><span>{t.record}</span><span>{t.diff}</span></div>)}</div></main><Footer/></>}
