import {matches} from '@/lib-data';import {SiteHeader,Footer} from '@/components/SiteHeader';export default function Page(){return <><SiteHeader/><main className="shell page-shell"><span className="eyebrow">2026 SEASON</span><h1>Schedule</h1><div className="list-card">{matches.map(m=><div className="match-row" key={m.home}><div><b>{m.date}</b><small>{m.time} · {m.course}</small></div><strong>{m.home} <em>vs</em> {m.away}</strong></div>)}</div></main><Footer/></>}
