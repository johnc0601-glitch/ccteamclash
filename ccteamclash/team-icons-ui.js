(()=>{const icons=window.CC_TEAM_ICONS||{},slug=s=>String(s||'').toLowerCase().replace(/['’]/g,'').replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'');function logo(name,cls='team-logo'){const svg=icons[slug(name)];return svg?`<span class="${cls}" aria-label="${name} logo">${svg}</span>`:''}function enhance(){document.querySelectorAll('.team-card').forEach(card=>{const name=card.querySelector('h2')?.textContent.trim(),mark=card.querySelector('.team-mark');if(name&&mark&&icons[slug(name)]&&!mark.querySelector('svg'))mark.innerHTML=logo(name,'team-logo team-logo-card')});const hero=document.querySelector('.profile-hero');if(hero){const name=hero.querySelector('h1')?.textContent.trim(),mark=hero.querySelector('.profile-mark');if(name&&mark&&icons[slug(name)]&&!mark.querySelector('svg'))mark.innerHTML=logo(name,'team-logo team-logo-hero')}document.querySelectorAll('.matchup .team-mark').forEach(mark=>{const name=mark.querySelector('b')?.textContent.trim();if(name&&icons[slug(name)]&&!mark.querySelector('svg'))mark.insertAdjacentHTML('afterbegin',logo(name,'team-logo team-logo-match'))})}const app=document.querySelector('#app');if(app)new MutationObserver(()=>requestAnimationFrame(enhance)).observe(app,{childList:true,subtree:true});window.addEventListener('hashchange',()=>setTimeout(enhance,20));enhance()})();