import {notFound} from 'next/navigation';import Link from 'next/link';import {stories} from '@/lib-data';import {SiteHeader,Footer} from '@/components/SiteHeader';
export function generateStaticParams(){return stories.map(s=>({slug:s.slug}))}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=stories.find(x=>x.slug===slug);if(!s)notFound();return <><SiteHeader/><main className="article shell"><Link href="/stories" className="back">← All stories</Link><span className="eyebrow">{s.category} · {s.date}</span><h1>{s.title}</h1><p className="article-lead">{s.excerpt}</p><div className={`article-image ${s.image}`}>ADD STORY PHOTO</div><div className="article-copy">{s.body.map(p=><p key={p}>{p}</p>)}{s.links?.map(l=><Link className="button" href={l.url} key={l.label}>{l.label}</Link>)}</div></main><Footer/></>}
