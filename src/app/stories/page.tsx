import Link from 'next/link';import {stories} from '@/lib-data';import {SiteHeader,Footer} from '@/components/SiteHeader';
export default function Page(){return <><SiteHeader/><main className="shell page-shell"><span className="eyebrow">STORY ARCHIVE</span><h1>League stories</h1><div className="story-grid">{stories.map(s=><article className="story-card" key={s.slug}><div className={`story-image ${s.image}`}><span>PHOTO</span></div><div className="story-body"><small>{s.category} · {s.date}</small><h3>{s.title}</h3><p>{s.excerpt}</p><Link href={`/stories/${s.slug}`}>Read story →</Link></div></article>)}</div></main><Footer/></>}
