import {teams} from '@/lib-data';import {SiteHeader,Footer} from '@/components/SiteHeader';export default function Page(){return <><SiteHeader/><main className="shell page-shell"><span className="eyebrow">THE LEAGUE</span><h1>Teams</h1><div className="team-grid">{teams.map((t,i)=><div className="team-card" key={t.name}><span>{String(i+1).padStart(2,'0')}</span><h2>{t.name}</h2><p>{t.record} record · {t.diff} differential</p></div>)}</div></main><Footer/></>}
